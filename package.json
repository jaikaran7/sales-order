{
  "name": "sales-order-app",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "^20.0.0 || ^22.0.0"
  },
  "dependencies": {
    "@sap/cds": "^8",
    "express": "^4",
    "@cap-js/sqlite": "^1"
  },
  "scripts": {
    "start": "cds-serve"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "credentials": {
          "database": ":memory:"
        }
      }
    }
  }
}