{
  "name": "sales-order-app",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@sap/cds": "^8"
  },
  "scripts": {
    "start": "cds run"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "credentials": {
          "database": ":memory:"
        }
      }
    }
  }
}